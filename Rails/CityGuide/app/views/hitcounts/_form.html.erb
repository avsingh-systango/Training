<%= form_for(hitcount) do |f| %>
  <% if hitcount.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hitcount.errors.count, "error") %> prohibited this hitcount from being saved:</h2>

      <ul>
      <% hitcount.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :hitable_id %>
    <%= f.number_field :hitable_id %>
  </div>

  <div class="field">
    <%= f.label :hitable_type %>
    <%= f.text_field :hitable_type %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
